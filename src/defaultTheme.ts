import { DefaultTheme } from "styled-components";

const defaultTheme: DefaultTheme = {
    colors: {
      primary: '#0066A1',
      secondary: 'green',
    },
    fonts: {
      primary: 'Arial, sans-serif',
    },
  };

export { defaultTheme }