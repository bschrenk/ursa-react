import { DefaultTheme } from "styled-components";

const defaultTheme: DefaultTheme = {
    colors: {
      primary: 'blue',
      secondary: 'green',
    },
    fonts: {
      primary: 'Arial, sans-serif',
    },
  };

export { defaultTheme }